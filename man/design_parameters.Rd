% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_parameters.R
\name{design_parameters}
\alias{design_parameters}
\title{Report the parameters a design can be redesigned on}
\usage{
design_parameters(design)
}
\arguments{
\item{design}{A \code{design} or a \code{design_step}.}
}
\value{
A data frame with one row per name per step: \code{name}, \code{value}
(a display snippet of the value),
\code{kind} (\code{scalar}, \code{vector}, \code{list}, \code{data}, \code{function} or \code{other}),
\code{declared} (whether a \code{declare_parameters()} step declares it), and
\code{step}. Rows are in step order.
}
\description{
Lists the names \code{\link[=redesign]{redesign()}} can change: the parameters a
\code{\link[=declare_parameters]{declare_parameters()}} step declares, and the objects the design's
expressions read out of the environments they were written in.

Symbols that resolve to a package (\code{rnorm}, \code{complete_ra}) are not
parameters and are left out, and neither are names that resolve to nothing,
because those are columns an earlier step created.
}
\examples{
design <- declare_parameters(n_units = 100) +
  declare_model(N = n_units, Y = rnorm(N))
design_parameters(design)
}
\seealso{
Other modifying a design:
\code{\link[=delete_step]{delete_step()}},
\code{\link[=design_notes]{design_notes()}},
\code{\link[=expand_design]{expand_design()}},
\code{\link[=insert_step]{insert_step()}},
\code{\link[=redesign]{redesign()}},
\code{\link[=replace_step]{replace_step()}}
}
\concept{modifying a design}
