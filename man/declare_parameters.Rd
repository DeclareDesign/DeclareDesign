% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_parameters.R
\name{declare_parameters}
\alias{declare_parameters}
\title{Declare the parameters of a design}
\usage{
declare_parameters(..., label = "parameters")
}
\arguments{
\item{...}{Named parameters. Each is evaluated once, in order, and may read
the parameters declared before it.}

\item{label}{Step label.}
}
\value{
A \code{design_step}.
}
\description{
Names the quantities a design is built from, so that \code{\link[=redesign]{redesign()}} changes
them and nothing else. A parameter declared here is bound for every step
that follows, and takes precedence over a binding of the same name in the
workspace.

Parameters are ordinary R expressions evaluated once, when the design is
built, and each may read the ones declared before it:
\code{declare_parameters(m_arms = 3, ks = seq_len(m_arms)[-1])}. They are not
redrawn on each simulation, which is what separates a parameter from a
random variable: \code{declare_parameters(u = rnorm(1))} fixes one number for
the life of the design.
}
\details{
Without a \code{declare_parameters()} step, a design's parameters are whatever
its expressions read out of the environment they were written in, and
\code{redesign()} finds them by name. That works and it stays supported:
\code{declare_model(N = n_units, ...)} with \code{n_units} defined outside the design
needs no declaration.

What does need one is a number written into the design. \code{declare_model(N = 500)} puts 500 in the design, and nothing outside it names that number, so
\code{redesign(design, N = 1000)} errors rather than rewriting the argument
because its name matched. Matching names is what used to make
\code{redesign(design, sd = 3)} write 3 into a column called \code{sd} instead of
changing the parameter behind it. Declaring separates the two: a redesign
changes the declared parameter, and a column of the same name is left
alone, because it belongs to the data rather than to the design.

The step generates no data and is skipped when the design runs.
}
\examples{
design <-
  declare_parameters(n_units = 100, effect = 0.5) +
  declare_model(N = n_units, U = rnorm(N), Y_Z_0 = U, Y_Z_1 = U + effect) +
  declare_inquiry(ATE = mean(Y_Z_1 - Y_Z_0)) +
  declare_assignment(Z = sample(rep(0:1, length.out = N))) +
  declare_measurement(Y = Y_Z_0 * (1 - Z) + Y_Z_1 * Z)

nrow(draw_data(design))
nrow(draw_data(redesign(design, n_units = 40)))

# A later parameter may read an earlier one.
design <- declare_parameters(m_arms = 3, ks = seq_len(m_arms)[-1]) +
  declare_model(N = 60) +
  declare_assignment(Z = complete_ra(N, conditions = seq_len(m_arms)))
design_parameters(design)
}
\seealso{
Other design declarations:
\code{\link[=declare_assignment]{declare_assignment()}},
\code{\link[=declare_estimator]{declare_estimator()}},
\code{\link[=declare_inquiry]{declare_inquiry()}},
\code{\link[=declare_measurement]{declare_measurement()}},
\code{\link[=declare_model]{declare_model()}},
\code{\link[=declare_notes]{declare_notes()}},
\code{\link[=declare_sampling]{declare_sampling()}},
\code{\link[=declare_step]{declare_step()}},
\code{\link[=declare_test]{declare_test()}}
}
\concept{design declarations}
