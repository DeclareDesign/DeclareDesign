% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{select_diagnosands}
\alias{select_diagnosands}
\title{Deprecated: select diagnosands by name}
\usage{
select_diagnosands(..., alpha = 0.05, subset = NULL, na.rm = FALSE)
}
\arguments{
\item{...}{Diagnosand names, optionally preceded by a \code{diagnosands} object
to subset.}

\item{alpha, na.rm}{Passed through when building from stock names.}

\item{subset}{Not supported. Filter the simulations table instead.}
}
\value{
A \code{diagnosands} object.
}
\description{
\code{select_diagnosands()} is retained so existing scripts keep running. Both of
its jobs have simpler spellings now. To build a set from the stock
definitions, pass their names to \code{\link[=diagnose_design]{diagnose_design()}} directly. To trim a set
of your own, declare the one you want, or join what you want to the defaults
with \code{+}.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{# old
diagnose_design(design, diagnosands = select_diagnosands("bias", "rmse"))
# new
diagnose_design(design, diagnosands = c("bias", "rmse"))
}\if{html}{\out{</div>}}
}
\examples{
suppressWarnings(select_diagnosands("bias", "rmse"))
}
