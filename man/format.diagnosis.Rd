% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print_methods.R
\name{format.diagnosis}
\alias{format.diagnosis}
\alias{reshape_diagnosis}
\title{Format a diagnosis for display}
\usage{
\method{format}{diagnosis}(x, digits = 2, ...)

reshape_diagnosis(diagnosis, digits = 2)
}
\arguments{
\item{x}{A \code{diagnosis} object.}

\item{digits}{Number of decimal places.}

\item{...}{Ignored.}

\item{diagnosis}{A \code{diagnosis} object.}
}
\value{
A \code{data.frame} of formatted strings.
}
\description{
Rounds the diagnosands, puts each bootstrap standard error in parentheses on
a second row beneath its estimate, and gives the columns display names
(\code{mean_estimand} becomes \verb{Mean Estimand}). This is what \code{\link[=print.diagnosis]{print.diagnosis()}}
shows and what you want in front of \code{knitr::kable()}.
}
\details{
The standard-error rows appear only when the diagnosis was bootstrapped. At
\code{bootstrap_sims = 0} there are no replicates, so the table is one row per
group and there are no parentheses.

It is a \code{format()} method because that is what it does. R's \code{format()} is
the generic for "the character representation this object prints as", and
\code{print()} is documented as calling it. The three views of a diagnosis are
worth keeping straight: \code{\link[=get_diagnosands]{get_diagnosands()}} is wide and numeric with
\code{se(bias)} as a sibling column, \code{\link[=tidy]{tidy()}} is long and numeric with the
standard error and interval as fields of each row, and this is wide and
character with the standard error rendered beneath its estimate. Only the
first two can be computed on. This one is a rendering of the first, a pure
function of it and \code{digits}, and it carries strictly less information than
either: the interval is not in it and the precision is gone.

\code{reshape_diagnosis()} is DeclareDesign's name for the same thing and calls
this method, so code written against DeclareDesign keeps working.

It returns a \code{data.frame} rather than a tibble because every column is a
formatted string, and a tibble would print a \verb{<chr>} row under a display
table.

Redesign parameter columns keep their own names, since \code{prob_each} is the
argument the reader passed and not a phrase to be title-cased.

DeclareDesign's \code{select} and \code{exclude} arguments are deliberately absent:
the result is a data frame, so \code{select()} already chooses columns from it,
and an argument that duplicates a verb is the wart this package exists to
remove.
}
\examples{
design <- declare_model(N = 30, Y = rnorm(N), Z = rep(0:1, 15)) +
  declare_inquiry(ATE = 0) +
  declare_estimator(Y ~ Z, .method = lm, term = "Z", inquiry = "ATE",
                    label = "ols")
d <- diagnose_design(design, sims = 5, bootstrap_sims = 0)
format(d)

# DeclareDesign's name for the same table
reshape_diagnosis(d)

# choose columns with select(), on the data frame it returns
format(d) |> dplyr::select(Term, Bias, Power)
}
