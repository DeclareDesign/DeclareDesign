% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_estimator.R
\name{declare_test}
\alias{declare_test}
\title{Declare a hypothesis test}
\usage{
declare_test(
  ...,
  .method = NULL,
  .summary = tidy_try,
  term = NULL,
  label = "test"
)
}
\arguments{
\item{...}{Arguments forwarded to \code{.method}. Typically the formula appears
first (e.g., \code{Y ~ Z}).}

\item{.method}{The model-fitting function. Defaults to
\code{\link[estimatr:lm_robust]{estimatr::lm_robust()}}.}

\item{.summary}{Function used to tidy \code{.method}'s output. Defaults to
\code{\link[=tidy_try]{tidy_try()}}.}

\item{term}{Optional character vector restricting which model terms appear
in the result.}

\item{label}{Step label. Defaults to \code{"estimator"}.}
}
\value{
A \code{design_step}.
}
\description{
Like \code{\link[=declare_estimator]{declare_estimator()}} but the result table does not gain an \code{inquiry}
column; intended for tests not tied to an estimand.
}
\examples{
design <- declare_model(N = 30, Z = rep(0:1, 15), Y = Z + rnorm(N)) +
  declare_test(Y ~ Z, .method = lm, term = "Z", label = "diff")
draw_estimates(design)
}
