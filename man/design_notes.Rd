% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_notes.R
\name{design_notes}
\alias{design_notes}
\title{Report the notes a design takes}
\usage{
design_notes(design)
}
\arguments{
\item{design}{A \code{design} or a \code{design_step}.}
}
\value{
A data frame with one row per note: \code{name}, \code{expr} and \code{step}.
}
\description{
Lists the names a \code{\link[=declare_notes]{declare_notes()}} step declares and the expression each
is computed from. Unlike \code{\link[=design_parameters]{design_parameters()}}, no value is reported: a
note has one only while the design is running, and a note computed from
data has a different one on every draw.

The two functions divide a design's names between them. What
\code{design_parameters()} lists can be changed with \code{\link[=redesign]{redesign()}}; what
\code{design_notes()} lists cannot, and follows from what can.
}
\examples{
design <- declare_parameters(m_arms = 3) +
  declare_notes(ks = seq_len(m_arms)[-1]) +
  declare_model(N = 60)
design_notes(design)
}
\seealso{
\code{\link[=design_parameters]{design_parameters()}}.

Other modifying a design:
\code{\link[=delete_step]{delete_step()}},
\code{\link[=design_parameters]{design_parameters()}},
\code{\link[=expand_design]{expand_design()}},
\code{\link[=insert_step]{insert_step()}},
\code{\link[=redesign]{redesign()}},
\code{\link[=replace_step]{replace_step()}}
}
\concept{modifying a design}
