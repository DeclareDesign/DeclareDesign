% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redesign.R
\name{redesign}
\alias{redesign}
\title{Re-parameterize a design}
\usage{
redesign(.design, ..., .expand = TRUE)
}
\arguments{
\item{.design}{A \code{design}. Named with a dot, like \code{.method} and \code{.summary}
in \code{\link[=declare_estimator]{declare_estimator()}}, because everything else here is a parameter of
the user's design: a plain \code{design} would partially match and swallow a
parameter named \code{d}, \code{de}, \code{des}, \code{desi} or \code{desig}, and designs with a
parameter named \code{d} exist.}

\item{...}{Named parameter values. A bare atomic vector supplies one design
per element; a data frame, a matrix, a function or any classed object is
one value. To sweep over such values, pass a list of them.}

\item{.expand}{If \code{TRUE} (default), expand the parameter grid; if \code{FALSE},
zip parallel vectors. Dotted for the same reason as \code{.design}: an
undotted \code{expand} after the dots would collide exactly with a parameter
of that name.}
}
\value{
A single \code{design} if one combination is supplied, otherwise a list
of designs named \code{design_1}, \code{design_2}, etc.
}
\description{
Replaces parameter values in the captured environments of a design's steps,
producing one or more modified designs. With \code{.expand = TRUE} (the default),
the cross-product of parameter values is taken; with \code{.expand = FALSE},
values are zipped position-wise.
}
\details{
A parameter that no step responds to draws a warning and is otherwise
ignored. \code{\link[=summary]{summary()}} on a design lists the names that are available.

An atomic vector always supplies one value per design, so a parameter that
is itself a vector has to be wrapped: \code{prob_each = list(c(0, .5, .5))} is
one design, where \code{prob_each = c(0, .5, .5)} is three. Handing a bare
vector to a parameter that currently holds one warns.

Only bare vectors and bare lists are read that way. A data frame, a matrix
and anything carrying a class are single replacement values, so a design
written as \code{declare_model(data = pilot, ...)} swaps its data with
\code{redesign(design, pilot = new_df)} and needs no wrapping. A redesign
reaches the name the design reads the data under, not \code{data}, which names
fabricate's argument and belongs to the declaration.
}
\examples{
designer <- function(N) {
  declare_model(N = N, Y = rnorm(N)) +
    declare_inquiry(mu = mean(Y))
}
design <- designer(50)
redesigned <- redesign(design, N = c(10, 20))
length(redesigned)

# a function-valued parameter
summarizer <- function(x) mean(x)
design <- declare_model(N = 50, Y = rnorm(N)) +
  declare_inquiry(mu = summarizer(Y))
redesign(design, summarizer = stats::median)
}
\seealso{
Other modifying a design:
\code{\link[=delete_step]{delete_step()}},
\code{\link[=design_notes]{design_notes()}},
\code{\link[=design_parameters]{design_parameters()}},
\code{\link[=expand_design]{expand_design()}},
\code{\link[=insert_step]{insert_step()}},
\code{\link[=replace_step]{replace_step()}}
}
\concept{modifying a design}
