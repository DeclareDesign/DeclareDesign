% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_inquiry.R
\name{declare_inquiry}
\alias{declare_inquiry}
\alias{declare_inquiries}
\alias{declare_estimand}
\alias{declare_estimands}
\title{Declare an inquiry}
\usage{
declare_inquiry(
  ...,
  subset = NULL,
  label = "inquiry",
  handler = NULL,
  draws = 1L
)

declare_inquiries(
  ...,
  subset = NULL,
  label = "inquiry",
  handler = NULL,
  draws = 1L
)

declare_estimand(
  ...,
  subset = NULL,
  label = "inquiry",
  handler = NULL,
  draws = 1L
)

declare_estimands(
  ...,
  subset = NULL,
  label = "inquiry",
  handler = NULL,
  draws = 1L
)
}
\arguments{
\item{...}{Named expressions. Each expression becomes an inquiry whose
numeric value is recorded in the simulation output.}

\item{subset}{Optional unquoted expression filtering the data before
estimands are computed.}

\item{label}{Step label. Defaults to \code{"inquiry"}.}

\item{handler}{Optional alternative handler. When supplied, \code{...} arguments
are evaluated against the data and passed to \code{handler()} rather than
being treated as named scalar inquiries; useful for vectorised inquiry
sets (for example, \code{handler = tibble}).}

\item{draws}{Number of nested draws for this step. When \verb{> 1}, the step is
re-executed \code{draws} times for each upstream draw during nested simulation.}
}
\value{
A \code{design_step}.
}
\description{
Each named argument records an inquiry: an expression evaluated on the data
as it stands when the step runs. Its value on a given draw is the estimand.
}
\examples{
design <-
  declare_model(N = 100, U = rnorm(N), Y_Z_0 = U, Y_Z_1 = U + 0.5) +
  declare_inquiry(ATE = mean(Y_Z_1 - Y_Z_0))

draw_estimands(design)

# Placed before declare_sampling(), the question is about the population;
# placed after, about the sample.
design <-
  declare_model(N = 500, Y = rnorm(N)) +
  declare_inquiry(population_mean = mean(Y)) +
  declare_sampling(S = sample(rep(0:1, length.out = N))) +
  declare_inquiry(sample_mean = mean(Y))

draw_estimands(design)
}
\seealso{
Other design declarations:
\code{\link[=declare_assignment]{declare_assignment()}},
\code{\link[=declare_estimator]{declare_estimator()}},
\code{\link[=declare_measurement]{declare_measurement()}},
\code{\link[=declare_model]{declare_model()}},
\code{\link[=declare_notes]{declare_notes()}},
\code{\link[=declare_parameters]{declare_parameters()}},
\code{\link[=declare_sampling]{declare_sampling()}},
\code{\link[=declare_step]{declare_step()}},
\code{\link[=declare_test]{declare_test()}}
}
\concept{design declarations}
