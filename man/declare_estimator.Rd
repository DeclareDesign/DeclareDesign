% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_estimator.R
\name{declare_estimator}
\alias{declare_estimator}
\title{Declare an estimator}
\usage{
declare_estimator(
  ...,
  .method = NULL,
  .summary = tidy_try,
  inquiry = NULL,
  term = NULL,
  label = "estimator",
  handler = NULL,
  draws = 1L
)
}
\arguments{
\item{...}{Arguments forwarded to \code{.method}. Typically the formula appears
first (e.g., \code{Y ~ Z}). A legacy \verb{model =} argument is read as \code{.method}
with a deprecation warning rather than forwarded.}

\item{.method}{The model-fitting function. Defaults to
\code{\link[estimatr:lm_robust]{estimatr::lm_robust()}}.}

\item{.summary}{Function used to tidy \code{.method}'s output. Defaults to
\code{\link[=tidy_try]{tidy_try()}}.}

\item{inquiry}{Either an inquiry label (character), a \code{design_step}, or a
list of these; used to join the estimate to its target estimand.}

\item{term}{Which model terms to report. A character vector keeps exactly
those terms, in that order, and errors if any is absent from the tidied
output; \code{TRUE} keeps every term including the intercept. When left
unset (or \code{FALSE}), only the first non-intercept term is reported, so
\code{declare_estimator(Y ~ Z + X)} returns the \code{Z} row alone. Evaluated on
every draw, so it may name a design parameter.}

\item{label}{Step label. Defaults to \code{"estimator"}.}

\item{handler}{Optional handler function. When supplied, the estimator
bypasses \code{.method}/\code{.summary} and instead calls
\code{handler(data, ...evaluated_dots...)}, which must return a tidy table.}

\item{draws}{Number of nested draws for this step. When \verb{> 1}, the step is
re-executed \code{draws} times for each upstream draw during nested simulation.}
}
\value{
A \code{design_step}.
}
\description{
Declares the answer strategy: a function that produces an estimate of an
inquiry from the observed data. The estimate is tidied to one row per term
and joined to its estimand by the \code{inquiry} label during diagnosis.
}
\examples{
design <- declare_model(N = 50, U = rnorm(N), Y = U) +
  declare_inquiry(mu = mean(Y)) +
  declare_estimator(Y ~ 1, .method = lm, term = "(Intercept)",
                    inquiry = "mu", label = "ols")
draw_estimates(design)
}
\seealso{
Other design declarations:
\code{\link[=declare_assignment]{declare_assignment()}},
\code{\link[=declare_inquiry]{declare_inquiry()}},
\code{\link[=declare_measurement]{declare_measurement()}},
\code{\link[=declare_model]{declare_model()}},
\code{\link[=declare_notes]{declare_notes()}},
\code{\link[=declare_parameters]{declare_parameters()}},
\code{\link[=declare_sampling]{declare_sampling()}},
\code{\link[=declare_step]{declare_step()}},
\code{\link[=declare_test]{declare_test()}}
}
\concept{design declarations}
