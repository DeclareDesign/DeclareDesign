% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_design.R
\name{get_estimates}
\alias{get_estimates}
\title{Get estimates for a fixed dataset}
\usage{
get_estimates(
  design,
  data = draw_data(design),
  start = 1L,
  end = length(design)
)
}
\arguments{
\item{design}{A \code{design}.}

\item{data}{A data frame.}

\item{start}{Integer; first step index to consider.}

\item{end}{Integer; last step index to consider.}
}
\value{
A tibble of estimates.
}
\description{
Runs only the estimator steps of a design against a supplied data frame.
}
\examples{
design <- declare_model(N = 30, U = rnorm(N), Z = rep(0:1, 15), Y = U + Z) +
  declare_estimator(Y ~ Z, .method = lm, term = "Z", label = "ols")
df <- draw_data(design)
get_estimates(design, df)
}
