% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnosands.R
\name{declare_diagnosands}
\alias{declare_diagnosands}
\title{Declare diagnosands}
\usage{
declare_diagnosands(..., alpha = 0.05)
}
\arguments{
\item{...}{Named expressions defining diagnosands.}

\item{alpha}{Significance level. Any diagnosand expression that mentions
\code{alpha} sees this value.}
}
\value{
A \code{diagnosands} object.
}
\description{
Defines summary statistics computed across simulations. Each named
expression is evaluated against the simulations data frame, grouped by
\code{inquiry}, \code{estimator}, \code{term}, and \code{outcome} where present.
}
\details{
Two diagnosands objects can be joined with \code{+}, which unions them and lets
a name declared twice take its later definition. Adding to the defaults and
overriding one of them are therefore the same act:

\if{html}{\out{<div class="sourceCode">}}\preformatted{default_diagnosands() + declare_diagnosands(mae = mean(abs(estimate - estimand)))
default_diagnosands() + declare_diagnosands(power = mean(p.value <= 0.1))
}\if{html}{\out{</div>}}

To compute diagnosands on some of the simulations rather than all of them,
filter the simulations table on the way in, the same way you would any
other data frame:

\if{html}{\out{<div class="sourceCode">}}\preformatted{simulate_design(design, sims = 500) |>
  filter(p.value <= 0.05) |>
  diagnose_design()
}\if{html}{\out{</div>}}
}
\examples{
diags <- declare_diagnosands(
  bias = mean(estimate - estimand, na.rm = TRUE),
  rmse = sqrt(mean((estimate - estimand)^2, na.rm = TRUE))
)
names(diags)

# power at the 10 percent level
declare_diagnosands(power = mean(p.value <= alpha), alpha = 0.1)
}
\seealso{
\code{\link[=default_diagnosands]{default_diagnosands()}} for the stock set, and \code{\link[=diagnose_design]{diagnose_design()}},
whose \code{diagnosands} argument also accepts a character vector of
\code{\link[=stock_diagnosand_names]{stock_diagnosand_names()}}.
}
