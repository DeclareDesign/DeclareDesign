% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print_methods.R
\name{print.design}
\alias{print.design}
\title{Print a design}
\usage{
\method{print}{design}(x, ...)
}
\arguments{
\item{x}{A \code{design}.}

\item{...}{Ignored.}
}
\value{
The input invisibly.
}
\description{
Lists the steps as the calls that declared them, then the parameters and
objects the design's expressions refer to: the names \code{\link[=redesign]{redesign()}} can
change, the value each one currently holds, its kind, and which steps
would respond to a change. A design with no such names prints its steps
alone. \code{\link[=summary]{summary()}} runs the design once as well and says what each step
did to the data.
}
\examples{
d <- declare_model(N = 30, Y = rnorm(N)) + declare_inquiry(mu = mean(Y))
print(d)
}
