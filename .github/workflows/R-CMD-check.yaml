# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
#
# This is the rewrite branch's own workflow. `main`'s triggers on
# [main, master] and would never fire here, and this branch needs a step
# `main` does not: two of its dependencies are resolved from the Remotes: field
# in DESCRIPTION rather than from CRAN. fabricatrZero is not on CRAN at all,
# and estimatr is, but at 1.0.6, which does not satisfy the `>= 2.0.0` this
# branch requires. Both remotes carry the `pkgname=` prefix; for estimatr that
# is deliberate insurance, since the repository name now collides with a real
# CRAN package and an unprefixed remote resolves in some pak configurations and
# not others.
on:
  push:
    branches: [rewrite]
  pull_request:
    branches: [rewrite]

name: R-CMD-check.yaml

permissions: read-all

# A push that supersedes a running check cancels it. The branch is worked on
# in bursts, and every run notifies the repository's watchers.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: ubuntu-latest,  r: 'release'}
          - {os: ubuntu-latest,  r: 'oldrel-1'}
          - {os: macos-latest,   r: 'release'}
          - {os: windows-latest, r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'
